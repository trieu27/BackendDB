<h3 class="lbl-atm">Danh sách các cây ATM
    <a class="btn btn-info btn-add" href="/atm/create"><i class="fa fa-plus"></i> Thêm mới</a>
    <a class="btn btn-primary btn-back" href="/dashboard"> Trở về</a></h3>

<table class="table table-bordered table-dark table-hover tbl-atm">
    <thead>
        <tr>
            <th>Tên đầy đủ</th>
            <th class="info-atm">Thông Tin</th>
            <th>Loại</th>
            <th>Đường dẫn ảnh</th>
            <th class="detail">Mô tả</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        {{#each list}}
        <tr>
            <td>{{this.Name}}</td>
            <td>{{this.Info}}</td>
            <td>{{this.Loai}}</td>
            <td>{{this.img_url}}</td>
            <td>{{this.detail}}</td>
            <td>
                <a class="btn btn-primary" href="/atm/modify/{{this._id}}"><i class="fa fa-pencil fa-lg"
                        aria-hidden="true"></i> Sửa</a>
                <a class="btn btn-success" href="/atm/delete/{{this._id}}"
                    onclick="return confirm('Bạn muốn xóa bản ghi này ?');"><i class="fa fa-trash fa-lg"
                        aria-hidden="true"></i> Xóa</a>
            </td>
        </tr>
        {{/each}}
    </tbody>
</table>