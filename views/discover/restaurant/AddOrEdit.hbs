<h3>{{viewTitle}}</h3>

<form action="/restaurant" method="POST" autocomplete="off">
    <input type="hidden" name="_id" value="{{restaurant._id}}">
    <div class="form-group">
        <label>Tên nhà hàng :</label>
        <input type="text" class="form-control" name="Name" placeholder="Nhập tên" value="{{restaurant.Name}}">
        <div class="text-danger">
            {{restaurant.NameError}}
        </div>
    </div>
    <div class="form-group">
        <label>Thông Tin :</label>
        <input type="text" class="form-control" name="Info" placeholder="Nhập thông tin" value="{{restaurant.Info}}">
        <div class="text-danger">
            {{restaurant.InfoError}}
        </div>
    </div>
    <div class="form-group">
        <label>Loại :</label>
        <select class="form-control" name="Loai">
            {{#each restaurantType}}
            <option>{{this.maLoai}}</option>
            {{/each}}
        </select>
    </div>
    <div class="form-group">
        <label>Đường dẫn ảnh 1 :</label>
        <input type="text" class="form-control" name="img_url1" placeholder="Nhập đường dẫn"
            value="{{restaurant.img_url1}}">
    </div>
    <div class="form-group">
        <label>Đường dẫn ảnh 2 :</label>
        <input type="text" class="form-control" name="img_url2" placeholder="Nhập đường dẫn"
            value="{{restaurant.img_url2}}">
    </div>
    <div class="form-group">
        <label>Mô tả :</label>
        <input type="text" class="form-control" name="detail" placeholder="Nhập mô tả" value="{{restaurant.detail}}">
    </div>
    <div class="form-group">
        <button type="submit" class="btn btn-info"> Xác nhận</button>
        <a class="btn btn-success" href="/restaurant/list"><i class="fa fa-list-alt"></i> Xem toàn bộ</a>
        <a class="btn btn-primary" href="/dashboard">Trở về</a>
    </div>
</form>